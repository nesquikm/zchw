#!/bin/sh

echo "Running pre-commit gate checks..."
echo ""

npm run typecheck && npm run lint && npm run test

if [ $? -ne 0 ]; then
  echo ""
  echo "Pre-commit gate check failed. Commit aborted."
  echo "Fix the issues above or use --no-verify to bypass (not recommended)."
  exit 1
fi
